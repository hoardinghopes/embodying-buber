{% extends "./notes-base.njk" %}

{% block content %}
  <header class="hero">
    <div class="hero-content">
      <div class="intro">
        {{ content | safe }}
      </div>
    </div>
  </header>

  <section class="post-list__wrapper">
    <h2 class="post-list__heading"></h2>

    {% set postslist = collections.twenty8days %}
    {% include "posts-card-list.njk" %}

  </section>

{% endblock content %}